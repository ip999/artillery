config:
  target: "http://0c199f8c.ngrok.io/"
  phases:
    - duration: 10
      arrivalRate: 1
scenarios:
  - flow:
      - log: "New virtual user running"
      - get:
          url: "/a"
      - get:
          url: "/b"
